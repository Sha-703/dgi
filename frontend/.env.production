# Charge par Vite uniquement lors du build de prod (`vite build` / Vercel).
# En dev, ce fichier est IGNORE : le frontend retombe sur http://localhost:8000/api.
# Valeur non secrete (URL publique de l'API backend).
VITE_API_URL=https://dgi-lesv.onrender.com/api
